MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=openscout
MYSQL_USER=openscout
MYSQL_PASSWORD=openscout
MYSQL_PORT=3306
REDIS_PORT=6379

OPSCOUT_COLLECTOR_BASE_URL=http://localhost:8081
OPSCOUT_MOCK_AGENT=true
OPSCOUT_COLLECTOR_MODE=mock

# Go Collector 可配置参数（阶段 5 GitHub 真实 API 集成）
# OPSCOUT_RATE_LIMIT_RPS=2            # GitHub API 每秒请求数，默认 2
# OPSCOUT_RATE_LIMIT_BURST=4          # burst 大小，默认 4
# OPSCOUT_CACHE_TTL_MINUTES=10        # 进程内缓存 TTL（分钟），默认 10
# OPSCOUT_WORKER_CONCURRENCY=4        # 批量采集并发数，默认 4
# OPSCOUT_HTTP_TIMEOUT_SECONDS=8      # HTTP 客户端超时（秒），默认 8

# Optional for real GitHub API mode.
GITHUB_TOKEN=<secret>

# LLM (DeepSeek V4 Pro) 配置（阶段 6 Spring AI Agent 编排）
# SPRING_AI_MODEL_CHAT 默认为 openai，未配置 DEEPSEEK_API_KEY 时自动降级为模板回答
SPRING_AI_MODEL_CHAT=openai
DEEPSEEK_API_KEY=<secret>
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# LLM 可配置参数
# OPSCOUT_LLM_ENABLED=true              # 设为 false 强制使用模板回答
# OPSCOUT_LLM_TIMEOUT_SECONDS=30        # LLM 调用超时（秒），默认 30
# OPSCOUT_LLM_MAX_TOKENS=2000           # 每次 LLM 回答最大 token 数，默认 2000
# OPSCOUT_LLM_TEMPERATURE=0.7           # LLM 温度参数，默认 0.7

# OpenScout V2 版本项目方案

> 本文档基于 V1（阶段 0–15）真实运行环境测试与代码审计结果编写，记录 V2 升级的优先修复项、架构演进路线和验收标准。
> V1 基线：`main` 分支，146 个 Java tests + Go tests 全部通过。
> **Phase 0 "Real User Ready" 已完成**：Docker 全栈部署、Web UI、多用户体系、线程安全等已合并到 V1 基线。

## 0. Phase 0 完成状态（2026-06-02）

### 已完成项

| 修复项 | 状态 | 说明 |
|---|---|---|
| Phase 0a: Dockerfiles + compose 全服务化 | ✅ | Java/Go 多阶段构建，4 服务 docker-compose |
| Phase 0a: Go 优雅关闭 | ✅ | signal.Notify + http.Server.Shutdown(10s) |
| Phase 0a: CORS + Swagger | ✅ | CorsConfig + springdoc-openapi |
| Phase 0a: Go 内存缓存 eviction | ✅ | 后台 eviction goroutine + maxEntries 上限 |
| Phase 0b: V2 数据库迁移 | ✅ | users 表 + user_id 列（NULLABLE 兼容已有数据） |
| Phase 0b: User Entity + Mapper | ✅ | UserEntity, UserMapper |
| Phase 0b: ApiKeyFilter DB 改造 | ✅ | DB 查用户 Key + userId request attribute 注入 |
| Phase 0b: 数据隔离 | ✅ | userId 全链路传播（trace → learning plan） |
| Phase 0b: Admin API | ✅ | POST/GET/DELETE /api/admin/users |
| Phase 0c: Web UI | ✅ | index.html SPA（SSE 进度 + 推荐 + 学习计划管理） |
| Phase 0c: AgentTrace 线程安全 | ✅ | CopyOnWriteArrayList |
| Phase 0c: AgentContext 并行安全 | ✅ | synchronized setter + unmodifiableList getter |
| Phase 0c: GoalInterpreter 重试 | ✅ | 空响应时自动重试 1 次 |
| Phase 0c: 回归验证 | ✅ | 146 Java tests + Go tests 通过 |

### 状态变更

以下 V1 审计项已在 Phase 0 中修复，从 V2 待办移除：

- ~~HIGH-1 AgentTrace 并行写入竞态~~ → CopyOnWriteArrayList
- ~~HIGH-2 并行步骤孤儿线程~~ → PlanExecutor 已有 future 管理（Phase 0 加固）
- ~~HIGH-3 Go Collector 无优雅关闭~~ → signal.Notify + Shutdown
- ~~MEDIUM-2 Go 内存缓存无上限~~ → eviction goroutine + maxEntries
- ~~MEDIUM-10 并发 LLM fallback 质量~~ → GoalInterpreter 重试

## 1. V1 现状总结

### 1.1 已完成功能

V1 完整实现了从 0 到 1 的 Agent 决策闭环，共 16 个阶段：

- **基础设施**（阶段 0–3）：Java Agent Server、Go Repo Collector、Docker Compose、端到端闭环
- **核心业务**（阶段 4–7）：MyBatis-Plus 持久化、GitHub 真实 API、Spring AI + DeepSeek V4 Pro、学习计划
- **Agent 决策闭环**（阶段 8–12）：Agent Runtime → Tool Runtime → Project Memory/RAG → Evidence ReAct → Reflection Verifier
- **工程化**（阶段 13–15）：Agent Events Stream (SSE)、Agent Evaluation、Production Hardening (Flyway/CI/API Key/Quota/配置治理)

### 1.2 V1 质量基线

| 维度 | 当前状态 |
|---|---|
| Java 测试 | 146 tests, 0 failures |
| Go 测试 | 3 packages, all passed |
| Agent Evaluation | 8 tests, 默认 mock fixture 通过 |
| Docker Compose | config 校验通过 |
| 配置项文档化 | `.env.example` 含 25+ 配置项 |
| GitHub Actions CI | Java/Go/Compose 三步 |
| 真实环境验证 | 28 场景 100% 通过 |

### 1.3 已识别的技术债务

详见第 2–3 节。所有发现均经过代码级复核确认。

---

## 2. V2 优先修复项（Phase 1: 安全加固）

> 以下每一项都经过 V1 真实环境测试和代码审计复核。标注了文件路径、行号和具体风险。

### 2.1 HIGH-1: AgentTrace 并行写入竞态

- **文件**: `openscout-agent-server/src/main/java/com/openscout/trace/AgentTrace.java:13`
- **代码**: `private final List<TraceToolCall> toolCalls = new ArrayList<>();`
- **触发路径**: `PlanExecutor.executeParallelGroup()` 并发执行 `generate_learning_plan` 和 `generate_answer` 时，两个线程同时调用 `TraceService.recordToolCall()` → `trace.getToolCalls().add(toolCall)`
- **风险**: `ArrayList` 非线程安全。并发 `add()` 可能导致 `ArrayIndexOutOfBoundsException`、数据丢失或内部数组损坏
- **V1 实际表现**: 5 并发请求中未触发（LLM 调用耗时远大于 trace 记录耗时，时序上极少真正重叠），但风险客观存在
- **修复方案**: 将 `toolCalls` 改为 `Collections.synchronizedList(new ArrayList<>())` 或 `CopyOnWriteArrayList`
- **修复文件**: `AgentTrace.java:13`, 同时检查 `TraceService.java:70`
- **验证方式**: 编写并发测试，同时从 3 个线程调用 `recordToolCall`，验证 toolCalls 数量和顺序

### 2.2 HIGH-2: 并行步骤失败后孤儿线程

- **文件**: `openscout-agent-server/src/main/java/com/openscout/agent/runtime/PlanExecutor.java:81-119`
- **代码**: `executeParallelGroup()` 方法在检测到 fatal 失败后抛出异常，但不取消正在运行的兄弟 future
- **风险**: 一个步骤失败时，另一个步骤继续运行并修改 `AgentContext` 状态（如 `setAnswer()`），之后再抛异常。调用方收到异常但 context 已被部分修改
- **修复方案**: 检测到 fatal 失败后调用 `future.cancel(true)` 取消其余 future
- **修复文件**: `PlanExecutor.java:111-118`
- **验证方式**: 单元测试：一个步骤抛 RuntimeException，验证另一个步骤被 `Thread.interrupted()` 中断

### 2.3 HIGH-3: Go Collector 无优雅关闭

- **文件**: `openscout-repo-collector/cmd/server/main.go:40-43`
- **代码**: `router.Run(":" + port)` 直接运行，无 `signal.Notify`、无 `http.Server.Shutdown()`
- **风险**: `kill` / `SIGTERM` 时正在处理的 GitHub API 请求被直接丢弃；Kubernetes pod 滚动更新时丢失请求
- **修复方案**:
  ```go
  srv := &http.Server{Addr: ":" + port, Handler: router}
  go func() { /* signal.Notify */ }()
  srv.ListenAndServe()
  srv.Shutdown(30 * time.Second)
  ```
- **修复文件**: `cmd/server/main.go`
- **验证方式**: 启动后 `kill -TERM`，验证进程等待现有请求完成后退出

### 2.4 HIGH-4: Java CollectorClient 零测试（关键集成点）

- **文件**: `openscout-agent-server/src/main/java/com/openscout/client/CollectorClient.java` (183 行)
- **风险**: 这是 Java ↔ Go 的唯一集成点。所有异常转换（403→RateLimitException、404→GitHubApiException、超时→CollectorUnavailableException）均为未测试代码路径
- **修复方案**: 使用 MockWebServer 覆盖 search/profile/readme/batchProfile 四种方法 × 五种响应（200/403/404/429/timeout）
- **修复文件**: 新建 `CollectorClientTest.java`
- **验证方式**: 20 个测试用例（4 方法 × 5 响应状态）

### 2.5 HIGH-5: Go GitHub Client 零测试

- **文件**: `openscout-repo-collector/internal/github/client.go` (约 120 行)
- **风险**: GitHub API 调用、限流解析、base64 解码、错误响应解析均无测试覆盖
- **修复方案**: 使用 `httptest.NewServer` 构建 mock GitHub API，覆盖 Search/GetRepo/GetReadme + 200/403/404/429/500 响应
- **修复文件**: 新建 `internal/github/client_test.go`
- **验证方式**: 至少 10 个测试用例

---

## 3. V2 建议修复项（Phase 2: 可观测性与健壮性）

### 3.1 MEDIUM-1: repo_info.description 截断风险

- **文件**: `openscout-agent-server/src/main/java/com/openscout/persistence/repo/RepoPersistenceService.java:54,69`
- **DDL**: `description VARCHAR(1000)`（`deploy/init.sql:10`）
- **现状**: `entity.setDescription(repo.description())` 无截断
- **对比**: `LearningPlanPersistenceService` 已使用 `truncate(plan.goal(), 1000)` 防御性截断
- **修复方案**: 在 `toRepoInfoEntity()` 和 `updateFromRepo()` 中添加 `truncate(desc, 1000)`
- **参考**: 已在 `项目实施进度.md` 第 6 节标记为已知遗留问题

### 3.2 MEDIUM-2: Go 内存缓存无上限增长

- **文件**: `openscout-repo-collector/internal/cache/memory.go`
- **现状**: 过期条目仅在读取时检测，过期后仍占用内存。无后台 eviction goroutine，无 maxSize 限制
- **风险**: 长期运行、多样化查询导致三个 map（repos/repo/readmes）无限增长
- **修复方案**: 添加 `maxEntries int` 上限 + 后台 TTL 扫描 goroutine（每 5 分钟），或改用支持 LRU 的缓存库

### 3.3 MEDIUM-3: EvidenceReActTool 过于复杂

- **文件**: `openscout-agent-server/src/main/java/com/openscout/agent/tool/EvidenceReActTool.java` (217 行)
- **现状**: `execute()` 方法 93 行、4 层嵌套、3 个循环/break 条件
- **修复方案**: 拆分为 `EvidenceGapDetector`（gap 检测）、`ReadmeEvidenceEnricher`（补查）、`RecommendationRescorer`（重评分）三个独立类

### 3.4 MEDIUM-4: RateLimitService 时钟与内存

- **文件**: `openscout-agent-server/src/main/java/com/openscout/config/RateLimitService.java`
- **时钟问题**（:33）: `System.currentTimeMillis()` 受 NTP 调整影响，时钟回拨时旧窗口永不过期
- **内存问题**（:15,42）: `MAX_MAP_SIZE = 10_000`，超过后才扫描过期条目。稳态 < 10000 key 时过期条目无限累积
- **修复方案**: 改用 `System.nanoTime()`（单调时钟）+ 定期后台清理（ScheduledExecutorService）

### 3.5 MEDIUM-5: MySQL LIKE 全表扫描

- **文件**: `openscout-agent-server/src/main/java/com/openscout/memory/ProjectMemoryService.java:69-79`
- **现状**: `WHERE full_name LIKE '%keyword%' OR description LIKE '%keyword%'` — 无索引可用，全表扫描
- **修复方案**: V2 引入向量检索（embedding + vector DB）替代；短期可选添加 MySQL FULLTEXT 索引

### 3.6 MEDIUM-6: Flyway baseline-on-migrate 风险

- **文件**: `openscout-agent-server/src/main/resources/application.yml:14-16`
- **配置**: `baseline-on-migrate: true`，无显式 `baseline-version`
- **风险**: 裸库 + `FLYWAY_ENABLED=true` 场景下，Flyway 将当前状态标记为 baseline version 1，**跳过 V1__init_schema.sql**。表不会被创建
- **缓解**: Docker Compose 场景下 `init.sql` 先创建表，Flyway 再 baseline，正常
- **修复方案**: 添加显式 `baseline-version: 1` + `baseline-description: "Docker init.sql baseline"`

### 3.7 MEDIUM-7: 配置文档缺口

- **缺失项**: `.env.example` 未包含以下 3 个配置项：
  | 环境变量 | 默认值 | 说明 |
  |---|---|---|
  | `OPSCOUT_MEMORY_FRESHNESS_HOURS` | `24` | Memory 缓存新鲜度（小时） |
  | `OPSCOUT_REACT_MAX_ROUNDS` | `1` | Evidence ReAct 最大补查轮数 |
  | `OPSCOUT_REACT_MAX_FOLLOW_UP_REPOS` | `3` | 单轮最多补查 repo 数 |
- **修复**: 补充到 `.env.example` 相应章节

### 3.8 MEDIUM-8: LLM 配置双通道

- **文件**: `openscout-agent-server/src/main/java/com/openscout/config/LlmConfig.java:31-38`
- **现状**: `@Value("${DEEPSEEK_API_KEY:}")` 等直接注入，绕过 `OpenScoutProperties` 统一配置类
- **风险**: 配置分散在两个入口（`OpenScoutProperties` 和 `LlmConfig`），两处不一致时行为未定义
- **修复方案**: 将 LLM 配置迁移到 `OpenScoutProperties.Llm` 内部类，`LlmConfig` 注入 `OpenScoutProperties`

### 3.9 MEDIUM-9: 持久化失败静默

- **模式**: 广泛存在于 `RepoPersistenceService`、`TraceService`、`ProjectMemoryService` 等
- **代码**: `catch (Exception e) { log.warn(...) }` — 不抛异常，不暴露给调用方
- **风险**: 持久化层长期故障无人察觉
- **修复方案**: 添加 `/actuator/health/persistence` 健康检查端点，定期探测 MySQL 连通性和最近写入时间

### 3.10 MEDIUM-10: 并发场景 LLM fallback 质量差

- **实测**: 5 并发请求中 2 个请求 GoalInterpreter 返回空响应，fallback 到原始中文问题作为 GitHub 搜索关键词 → 0 结果
- **根因**: DeepSeek API 在并发下偶发超时/空响应
- **修复方案**: GoalInterpreter 添加 1 次重试（不同 temperature）+ fallback 到简单关键词提取（中英文分词）

---

## 4. V2 架构演进路线

### Phase 3: 智能检索升级

| 任务 | 说明 | 依赖 |
|---|---|---|
| 向量检索 | 用 embedding 替代 MySQL LIKE，提升关键词搜索精度和性能 | Phase 1 完成后 |
| 全文检索增强 | 在向量检索就绪前，MySQL FULLTEXT 作为过渡方案 | 可选 |
| 语义 Memory | 基于 embedding 相似度判断 memory 复用，替代纯关键词匹配 | 向量检索完成后 |

### Phase 4: 分布式与多用户

| 任务 | 说明 | 依赖 |
|---|---|---|
| Redis 分布式 Quota | 替代进程内 `RateLimitService`，支持多实例 | Phase 1 完成后 |
| 用户体系 | 注册/登录/JWT，替代当前的 API Key 门卫模式 | Phase 2 完成后 |
| RBAC 权限 | 角色管理、租户隔离 | 用户体系完成后 |
| 跨实例 SSE | Redis Pub/Sub 广播事件，支持多实例部署 | Redis Quota 完成后 |

### Phase 5: Agent 能力升级

| 任务 | 说明 | 依赖 |
|---|---|---|
| 动态 Tool Planning | Spring AI `@Tool` 注解 + LLM 动态选择工具 | Phase 3 完成后 |
| MCP 协议 | Model Context Protocol，标准化外部工具接入 | 动态 Tool Planning 完成后 |
| 多轮对话 | 对话历史管理 + conversation_id 关联 | Phase 4 完成后 |
| 项目对比报告 | 原阶段 8 延后项，需稳定的 evidence + verifier | Phase 3 完成后 |

### Phase 6: 生产就绪

| 任务 | 说明 | 依赖 |
|---|---|---|
| Prometheus 指标 | JVM、HTTP、LLM 延迟、GitHub API 调用统计 | Phase 2 完成后 |
| 分布式 Tracing | traceId 跨服务关联（Java → Go → GitHub API） | Phase 2 完成后 |
| Kubernetes Helm Chart | 生产级部署描述 | Phase 4 完成后 |
| 线上压测 | 真实负载下的性能基线和瓶颈分析 | 所有 Phase 完成后 |

---

## 5. V2 实施原则

1. **向后兼容**: `/api/agent/ask` 响应字段不变，现有 API 不破坏
2. **Feature Flag**: 新能力默认关闭（`enabled: false`），与 V1 行为一致
3. **先测试、后代码**: 每项修复先编写复现测试，再实施修复
4. **每 Phase 独立分支**: 沿用 V1 的分支策略（`feature/XX-xxx` → propose → apply → review → achieve → merge）
5. **真实环境验证**: 每 Phase 完成后在真实 GitHub + LLM 环境下跑完整 28 场景验证
6. **数据库迁移**: Schema 变更使用 Flyway `V2__xxx.sql`，与 V1 基线兼容
7. **文档同步**: `code_copilot/knowledge/index.md`、`README.md`、`.env.example` 随代码同步更新

---

## 6. V2 验收标准

### 6.1 Phase 1 验收（安全加固）

- [ ] AgentTrace 并发写入测试通过（3 线程同时 recordToolCall）
- [ ] 并行步骤孤儿线程取消测试通过
- [ ] Go Collector graceful shutdown 测试（`kill -TERM` 后 30s 内完成现有请求）
- [ ] Java CollectorClient 测试 ≥ 20 个
- [ ] Go GitHub Client 测试 ≥ 10 个
- [ ] 现有 146 Java tests + Go tests 回归通过
- [ ] Agent Evaluation 默认命令通过
- [ ] `verify-local.sh` 4/4 通过

### 6.2 Phase 2 验收（可观测性与健壮性）

- [ ] 所有 10 项 MEDIUM 修复完成或标记为已知取舍
- [ ] `/actuator/health/persistence` 端点可报告 MySQL 连通性
- [ ] `.env.example` 包含全部配置项
- [ ] `OpenScoutProperties` 统一管理所有配置
- [ ] Go 内存缓存有大小上限和后台清理

### 6.3 Phase 3–6 验收

- [ ] 每个 Phase 独立分支合并前通过完整回归测试
- [ ] 真实环境 28 场景验证通过
- [ ] 新功能有对应的 `code_copilot/changes/<id>/` 文档

---

## 7. 变更记录

| 日期 | 变更 | 结果 |
|---|---|---|
| 2026-06-02 | 基于 V1 真实环境测试 + 代码审计创建 V2 方案 | — |

---

## 附录 A: V1 审计方法

本次审计覆盖以下维度：

- **真实环境测试**: 28 个测试场景（API 端点、LLM 调用链路、数据库持久化、并发、边界条件）
- **代码审计**: 全部 Java 生产类（~54 个）+ Go 模块（7 个）
- **数据库审计**: 5 张表的 Schema 一致性、Flyway vs init.sql 差异、索引覆盖
- **配置审计**: `application.yml` ↔ `.env.example` ↔ `OpenScoutProperties` 三方对照
- **测试覆盖**: Java 测试类（32 个）与生产类的映射关系

## 附录 B: 已知取舍（非问题）

以下项在审计中讨论过，但确认为有意设计选择，不作为 V2 修复项：

1. **`AgentEvaluationCommandTest` 使用真实组件**（非纯 mock）: 这是有意设计，用于捕获集成问题
2. **持久化默认关闭**（`OPSCOUT_PERSISTENCE_ENABLED=false`）: 确保本地 Demo 零依赖
3. **API Key/Quota 默认关闭**: 确保本地开发和 CI 不退化
4. **`generate_answer` 仅覆盖 Top 5 项目**: LLM 上下文窗口限制
5. **Go Collector 的日志静默**: GitHub client 错误由调用方处理，避免重复日志

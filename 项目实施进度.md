# OpenScout 项目实施进度

> 本文档用于跟踪 OpenScout 从 0 到 1 的实施方案、阶段进度和分支规划。  
> 后续每完成一个阶段，都需要同步更新本文档，并在进入下一阶段前确认范围、验收标准和风险。
> 本文档是当前唯一执行入口；专项方案可以作为输入，但落地顺序、分支和验收口径以本文档为准。

## 1. 使用原则

- 每一个实施阶段对应一个独立 Git 分支。
- 每个阶段先明确目标、范围、验收标准，再进入编码或文档修改。
- 阶段完成后需要记录验证结果、未完成项、后续调整建议。
- `main` 只作为稳定基线分支；功能开发、验证、修复和方案调整都在分支中完成。
- `code_copilot/changes/` 记录具体 change 的 spec、tasks、test-spec、log；本文档记录项目整体路线和跨阶段进度。
- `OpenScout Agent 项目方案.md` 是原始方案输入文件，不加入 Git。
- `Agent推理架构优化方案.md` 已吸收进本文档的阶段 8-15；若继续保留，只作为架构草案和溯源材料，不再作为独立执行入口。

## 2. 当前 Git 状态

- 当前分支：`feature/11-reflection-verifier`
- 当前用途：阶段 12 Reflection Verifier 完成，待合并回 `main`。
- 当前仓库状态：阶段 12 `feature/11-reflection-verifier` apply/review/achieve 全部完成，7 轮 review 修复 28 项问题，98 tests 通过。
- 当前 Git 忽略规则：`OpenScout Agent 项目方案.md`、`.tools/`、`.vscode/`、`target/` 等本地文件已通过 `.gitignore` 排除。
- 最近提交：
  - `62f63d3 feat: 阶段 11 Evidence ReAct`
  - `f27f21f feat: 阶段 10 Project Memory / RAG`

## 3. 阶段状态说明

| 状态 | 含义 |
|---|---|
| 待确认 | 阶段目标和验收标准还需要确认 |
| 待开始 | 阶段已确认，但尚未创建或切换到对应分支实施 |
| 进行中 | 已在对应分支实施 |
| 待验证 | 代码或文档已完成，但验证命令未全部通过 |
| 已完成 | 阶段目标完成，验证结果已记录 |
| 已合并 | 阶段分支已合并回 `main` |
| 延后 | 暂不进入当前从 0 到 1 主线 |

## 4. 从 0 到 1 实施路线

| 步骤 | 分支 | 阶段目标 | 当前状态 | 验收标准 |
|---|---|---|---|---|
| 0 | `plan/implementation-progress` | 建立总进度文档和分支规划 | 已合并 | 根目录存在本文档，后续阶段有清晰分支和验收口径 |
| 1 | `feature/00-mvp-foundation` | 创建 Java Agent Server、Go Repo Collector、部署脚本、README 和 Demo 文档 | 已完成 | Java `mvn test` 通过；Docker Compose config 通过；Go 工具链验证补齐 |
| 2 | `feature/01-go-collector-verification` | 安装/确认 Go 工具链后，格式化并验证 Go Collector | 已完成 | `gofmt -w cmd internal`、`go test ./...` 通过；Go 服务启动验证并入步骤 3 |
| 3 | `feature/02-mock-e2e-demo` | 跑通 Java 调 Go 的 mock 端到端演示 | 已完成 | Docker config、Java/Go 单测、Go `/api/repos/mock`、Java `/api/agent/ask`、Trace 查询和 Collector 不可用 502 场景均已验证 |
| 4 | `feature/03-mybatis-persistence` | 将项目画像、分析结果和 Trace 从内存实现推进到 MyBatis-Plus 持久化 | 已完成 | agent_trace/repo_info/repo_analysis 落库；MySQL 集成验证通过 |
| 5 | `feature/04-github-real-api` | 完善真实 GitHub API 模式，包括 search/profile/readme、限流、错误处理和缓存策略 | 已完成 | 无 Token mock 可用；有 Token 真实 API 可用；403/429 处理清晰 |
| 6 | `feature/05-spring-ai-deepseek-agent` | 接入 Spring AI + DeepSeek V4 Pro，将模板回答替换为 LLM 自然语言生成 | 已完成 | 未配置 Key 时可 fallback 到模板；配置 Key 后生成自然语言推荐回答 |
| 7 | `feature/06-learning-plan` | 生成并持久化学习路径与任务 | 已合并 | 根据推荐项目生成 7 天任务，支持查询和状态更新 |
| 8 | `feature/07-agent-runtime` | 把当前固定流程抽象为 Agent Runtime 内核，显式记录 plan、step、observation 和执行状态 | 已合并 | `/api/agent/ask` 响应兼容现有字段；mock/real 模式走同一套 executor；Trace 可查看 plan/step/observation；Planner、Executor、异常和 fallback 单测通过 |
| 9 | `feature/08-tool-runtime` | 将 Collector 调用、评分和学习计划能力标准化为 Tool Runtime | 已合并 | `AgentService` 通过 Tool Runtime 调用能力；Tool 输入输出可序列化并进入 Trace；限流、超时、404、部分失败有结构化错误 |
| 10 | `feature/09-project-memory-rag` | 建立 Project Memory / RAG，复用历史项目知识并做 freshness 判断 | 已合并 | 同 repo 可复用已有画像和 README 摘要；Trace 展示 memory hit/miss/freshness/write-back；无向量库时 MySQL 关键词检索可跑通 |
| 11 | `feature/10-evidence-react` | 基于 evidence gap 追加有限工具调用，补齐推荐证据 | 已合并 | Trace 展示 evidence gap、follow-up action、observation；README 缺失、限流、部分失败有测试；循环有最大轮数和停止条件 |
| 12 | `feature/11-reflection-verifier` | 在最终回答前加入自检，拦截 LLM 编造和不可执行学习计划 | 已完成 | verifier 可发现分数篡改、无证据项目能力和学习计划幻觉；Trace 展示 verifier 结果；无模型 Key 时仍 fallback |
| 13 | `feature/12-agent-events-stream` | 支持 Agent run 与 SSE 事件流，展示计划、工具、观察、补查和自检过程 | 待开始 | 支持 run 查询和事件流；事件与 traceId 关联；失败、限流、fallback 有事件；事件不输出敏感数据和大文本 |
| 14 | `feature/13-agent-evaluation` | 建立可复现 Agent 评测集和指标报告 | 待开始 | 有固定评测命令；覆盖推荐相关性、evidence 覆盖、hallucination 拦截、memory hit、fallback、延迟和 GitHub API 调用节省 |
| 15 | `feature/14-production-hardening` | 补齐迁移、CI、演示脚本、鉴权、配额和配置治理等工程化能力 | 待开始 | 新环境可按文档稳定启动；schema 变更可迁移；CI 覆盖 Java/Go；mock、真实 GitHub、LLM enabled/disabled 边界清楚 |

## 5. 当前已完成事实

以下事实来自当前工作区、`code_copilot/changes/openscout-mvp-foundation` 和 `code_copilot/changes/openscout-mock-e2e-demo`：

- 已创建 `code_copilot/` 工作区、根级 `AGENTS.md`、`CLAUDE.md`。
- 已创建 `.gitignore`，排除 `/OpenScout Agent 项目方案.md`。
- 已创建 Java 服务 `openscout-agent-server/`。
- 已创建 Go 服务 `openscout-repo-collector/`，Go module 为 `github.com/LiPeicheng/openscout-repo-collector`。
- 已创建 `deploy/docker-compose.yml` 和 `deploy/init.sql`。
- 已创建根级 `README.md` 和 `docs/` 演示文档。
- Java 验证 `cd openscout-agent-server && mvn test` 已通过。
- Docker Compose 配置验证 `docker compose -f deploy/docker-compose.yml config` 已通过。
- 已在项目本地 `.tools/go` 安装 Go `1.26.3`，并通过 `.gitignore` 排除工具目录。
- 已创建 `scripts/use-local-tools.sh`，用于启用项目本地 Go、`GOCACHE` 和 `GOPATH`。
- Go 验证 `gofmt -w cmd internal` 已通过。
- Go 验证 `cd openscout-repo-collector && go test ./...` 已通过。
- Docker CLI 和 Docker Compose 当前已可用，版本分别为 Docker `28.4.0`、Docker Compose `v2.39.2-desktop.1`。
- 阶段 3 已完成 Java 调 Go mock 端到端验证：Go `/health`、Go `/api/repos/mock`、Java `/api/agent/ask` 和 Java `/api/agent/traces/{traceId}` 均通过。
- 阶段 3 已验证 Collector 不可用场景：停止 Go 后 Java `/api/agent/ask` 返回 HTTP 502，并保留失败 `traceId` 和错误摘要。
- 阶段 3 为 mock 模式修复 Spring AI 无 Key 启动阻塞：默认通过 `SPRING_AI_MODEL_* = none` 关闭模型 provider，后续真实 DeepSeek 接入时再显式开启。
- `code_copilot/changes/openscout-mvp-foundation/` 和 `code_copilot/changes/openscout-mock-e2e-demo/` 均已归档为 `done`。
- 阶段 3 的可复用结论已沉淀到 `code_copilot/knowledge/index.md`。

## 6. 下一步确认点

阶段 11 已完成 apply/review/review fix/复查 fix/achieve，后续需要确认：

- 是否将阶段 11 分支合并回 `main`，再进入阶段 12 `feature/11-reflection-verifier`。
- Project Memory 当前仅用 MySQL LIKE 关键词检索；向量检索和 FULLTEXT 索引作为后续优化项。
- Reflection Verifier、Agent Events Stream 和 Agent Evaluation 仍按阶段 12-14 拆分，不在阶段 11 插队。

## 7. Agent 决策闭环升级主线

阶段 8-15 的合并原则：

- 优先建设 Agent 决策闭环：计划、工具、观察、记忆、补查、自检、追踪。
- 规则评分仍是最终分数来源，LLM 不得覆盖规则评分。
- Planner、Tool、Memory、ReAct、Verifier 都必须可追踪、可限制、可 fallback。
- 新能力默认保持 `/api/agent/ask` 兼容，避免破坏阶段 7 已完成的学习计划闭环。
- 每个阶段仍需创建独立 `code_copilot/changes/<change-id>/`，先 propose，用户确认后再 apply。

推荐执行顺序：

1. `openscout-agent-runtime`
2. `openscout-tool-runtime`
3. `openscout-project-memory-rag`
4. `openscout-evidence-react`
5. `openscout-reflection-verifier`
6. `openscout-agent-events-stream`
7. `openscout-agent-evaluation`
8. `openscout-production-hardening`

原阶段 8 `feature/07-project-compare-report` 调整为延后项：

- 延后原因：项目对比和报告输出依赖更稳定的 evidence、Tool Runtime、Memory 和 verifier；过早实现会把复杂度堆在回答层。
- 后续触发条件：阶段 12 verifier 完成后，若已有稳定 evidence 结构和报告字段，再作为报告增强 change 重新 propose。

## 8. 上一阶段记录

```text
阶段：3 - mock 端到端演示
分支：feature/02-mock-e2e-demo
目标：跑通 Go Collector mock API、Java Agent ask API 和 Trace 查询。
范围：本地启动与 curl 验证；必要时修复阻塞演示的最小问题；同步 code_copilot change 和进度记录。
不做：MyBatis 持久化、真实 GitHub API、Spring AI DeepSeek、Redis adapter、前端和生产部署。
风险：端口占用、跨服务配置、长驻进程残留、Trace 内存生命周期、mock 结果被误写成真实 API 或模型验证。
验证命令：docker compose config；Java mvn test；Go go test；curl /health；curl /api/repos/mock；curl /api/agent/ask；curl /api/agent/traces/<traceId>。
开始时间：2026-05-30
完成时间：2026-05-30
结果：Docker Compose config 通过；Go /health 和 /api/repos/mock curl 通过；Java /api/agent/ask curl 通过；Trace 查询通过；Collector 不可用时 Java 返回 502 和失败 traceId；openscout-mvp-foundation 和 openscout-mock-e2e-demo 均已归档为 done；阶段知识已沉淀到 code_copilot/knowledge/index.md。
未完成项：无；Trace 持久化、真实 GitHub API、Redis adapter 和 Spring AI DeepSeek 接入属于后续阶段。
下一步：进入阶段 4 前确认 MyBatis-Plus 持久化范围、表结构和验证口径。
```

## 9. 当前阶段记录

```text
阶段：12 - Reflection Verifier
分支：feature/11-reflection-verifier
目标：在最终回答前加入自检，拦截 LLM 编造和不可执行学习计划。
范围：VerifyAnswerTool；分数完整性检查（回答分数 vs ProjectScore）；证据声明检查（回答声称 vs evidence）；学习计划可行性检查（任务仓库名 vs 推荐列表）；openscout.verifier.* 配置；Trace verify_* 事件；单元测试与回归测试；文档同步。
不做：LLM 自动修正问题（仅报告）；Verifier 阻断 ask 响应（continueOnFailure=true）；LLM 语义检查（第一版 llm-enabled=false）；GoalInterpreter 自检；SSE/Streaming；Agent Evaluation；向量检索/FULLTEXT/embedding；新增 DDL；前端和生产鉴权。
风险：规则检查误报；分数提取正则覆盖不全；continueOnFailure=true 可能漏过编造内容。
验证命令：Java mvn test；Go go test ./...；Docker compose config。
开始时间：2026-06-01
完成时间：2026-06-01
结果：已完成 apply/review/achieve；新增 VerifyAnswerTool（含 ScoreIntegrityChecker、EvidenceClaimsChecker、LearningPlanChecker、VerificationResult、VerificationIssue、VerifierUtils）、Planner step verify_answer、openscout.verifier.* 配置、Trace verify_completed 事件；7 轮 review 共发现并修复 28 项问题（含否定声明误匹配、短名冲突、重复代码消除、DRY 重构等）；Java 98 tests、Go tests 均通过；阶段知识已沉淀到 code_copilot/knowledge/index.md。
未完成项：LLM 语义检查（llm-enabled=false 第一版关闭）、自动修正问题、GoalInterpreter 自检、SSE/Streaming、Agent Evaluation 按 spec 留到后续阶段。
下一步：合并回 main，进入阶段 13 feature/12-agent-events-stream proposal。
```
范围：EvidenceGap/EvidenceGapDetector；ReadmeEvidenceEnricher；EvidenceReActTool；RecommendationScoringService；RuleBasedAgentPlanner 插入 `evidence_react`；`openscout.react.*` 配置；Trace gap/follow-up/observation/rescore/stop 事件；README 和 code_copilot 文档同步；单元与回归测试。
不做：Spring AI 动态 Tool Calling、MCP、SSE/Streaming、Reflection Verifier、Agent Evaluation、向量检索、FULLTEXT、embedding、新增 DDL、前端和生产鉴权；第一版不补 release、目录结构、issues。
风险：GitHub API 限流；补查循环受默认 1 轮、3 repo 控制；补查后评分与 memory 回写已复用 RecommendationScoringService；Trace 只记录摘要和错误原因；新增 step 改变 Trace 顺序但不改 ask 响应字段。
验证命令：Java mvn test；Go go test ./...；Docker compose config。
开始时间：2026-06-01
完成时间：2026-06-01
结果：已完成 apply/review/review fix/复查 fix/achieve；新增 EvidenceGapDetector、ReadmeEvidenceEnricher、EvidenceReActTool、RecommendationScoringService；Planner mock/real 计划均插入 `evidence_react`；新增 `OPSCOUT_REACT_ENABLED`、`OPSCOUT_REACT_MAX_ROUNDS`、`OPSCOUT_REACT_MAX_FOLLOW_UP_REPOS`；review fix 已处理 README 失败同请求去重、空 README `empty_readme`、`fetch_readme` rate limit 停止和 retryAfter 传递；Java 70 tests、Go tests、Docker compose config 均通过；阶段知识已沉淀到 `code_copilot/knowledge/index.md`。
未完成项：Reflection Verifier、SSE/Streaming、Agent Evaluation、向量检索、FULLTEXT、release/tree/issues 补查、生产鉴权均属于后续阶段；当前无阶段 11 阻塞 deferred。
下一步：进入阶段 12 Reflection Verifier proposal。
```

### 上一阶段记录

```text
阶段：10 - Project Memory / RAG
分支：feature/09-project-memory-rag
目标：建立 Project Memory 层，通过 MySQL 关键词检索复用历史项目画像和 README 摘要，减少重复 GitHub API 调用。
范围：ProjectMemoryService（MySQL LIKE 关键词搜索 + repo_analysis 缓存查询 + freshness 判断）；CheckMemoryTool（先于 search_repos 执行）；RuleBasedAgentPlanner 插入 check_memory 步骤；SearchReposTool memory 命中跳过 Collector；FetchReadmeTool README 缓存命中跳过 GitHub；ScoreProjectsTool 持久化后记录 memory_writeback；AgentContext memory 状态字段；openscout.memory.* 配置；7 个新 Trace 事件（memory_check/memory_hit/memory_miss/search_repos_skipped/readme_cache_hit/memory_writeback）；单元测试（50 tests）和文档同步。
不做：向量数据库、FULLTEXT 索引、embedding 生成、Evidence ReAct、Reflection Verifier、SSE/Streaming、公开 Tool API、MCP、前端、生产鉴权、新增 DDL。
风险：MySQL LIKE 大数据量性能（当前极小，无实际风险）；freshness TTL 全局单一阈值；memory 事件继续复用 TraceToolCall；check_memory 作为固定 Tool 插入计划。
验证命令：Java mvn test；Go go test ./...；Docker compose config。
开始时间：2026-05-31
完成时间：2026-05-31
结果：已创建 code_copilot/changes/openscout-project-memory-rag/ proposal 并完成 apply/review/achieve；新增 ProjectMemoryService、CheckMemoryTool、AgentContext memory 字段、OpenScoutProperties.Memory 配置；修改 RuleBasedAgentPlanner（mock 6 step / real 7 step）、SearchReposTool（memory 跳过）、FetchReadmeTool（README 缓存 + hasExamples 正则修复 + readmeLength 推断 + hasDocker DRY）、ScoreProjectsTool（memory_writeback）；50 tests Java + Go/Docker 回归通过；未新增 DDL。
未完成项：向量检索、FULLTEXT 索引、embedding、ReAct、Verifier、SSE、MCP、生产鉴权，均属于后续阶段；review 低/中风险为 N+1 DB 查询和双 Tool 隐式契约。
下一步：进入阶段 11 feature/10-evidence-react proposal。
```

### 上一阶段记录

```text
阶段：9 - Tool Runtime
分支：feature/08-tool-runtime
目标：将 Collector 调用、评分、学习计划和回答生成能力标准化为 Tool Runtime，让 Agent Runtime 通过 Tool Registry/Executor 调用能力。
范围：Tool Runtime 核心契约；Tool Registry/Executor；interpret_goal/search_repos/fetch_readme/score_projects/generate_learning_plan/generate_answer Tool；Tool 输入输出摘要；Trace Tool 事件；Registry/Executor/Tool/PlanExecutor 回归测试；文档同步。
不做：Project Memory / RAG、Evidence-aware ReAct、Reflection Verifier、SSE/Streaming、公开 Tool API、Spring AI @Tool、MCP、前端、生产鉴权、多用户隔离、数据库迁移框架。
风险：/api/agent/ask 响应兼容；Tool 抽象过度设计；真实 GitHub API 限流/404/部分失败语义退化；LLM fallback；Trace 摘要脱敏；Tool 错误 recoverable/fatal 边界。
验证命令：Java mvn test；Go go test ./...；Docker compose config；可选 curl 验证 ask 后 Trace 包含 plan/step/observation 和 Tool Runtime 摘要。
开始时间：2026-05-31
完成时间：2026-05-31
结果：已创建 code_copilot/changes/openscout-tool-runtime/ proposal 并完成 apply/review/achieve；新增 Tool Runtime 契约、Registry/Executor、6 个固定 Tool、Trace agent_tool_* 事件、PlanExecutor 委托 Tool Runtime、ToolExecutor/PlanExecutor 测试和文档同步；阶段知识已沉淀到 code_copilot/knowledge/index.md；未新增 DDL。
未完成项：未做 Memory/RAG、ReAct、Verifier、SSE、Spring AI @Tool、MCP、生产鉴权，均属于后续阶段；review 记录的低风险残留为 fatal exception 分支 step observation latency 为 0，但 agent_tool_failed 保留真实耗时。
下一步：进入阶段 10 feature/09-project-memory-rag proposal。
```

### 上一阶段记录

```text
阶段：8 - Agent Runtime 内核
分支：feature/07-agent-runtime
目标：把当前固定 ask 编排抽象为显式 Agent Runtime，记录 plan、step、observation 和执行状态。
范围：AgentPlan/PlanStep/StepObservation/AgentContext；规则模板 Planner；PlanExecutor；AgentService 委托 Runtime；Trace plan/step/observation 摘要；Planner/Executor/Trace/回归测试；文档同步。
不做：项目对比报告、前端、RAG、Project Memory、Evidence-aware ReAct、Reflection Verifier、SSE/Streaming、Spring AI Tool Calling、MCP、生产鉴权、多用户隔离、数据库迁移框架。
风险：/api/agent/ask 响应兼容；真实 GitHub API 限流/失败处理；LLM fallback；Trace 摘要脱敏和持久化兼容；重构后 mock/real 链路不能退化。
验证命令：Java mvn test；Go go test ./...；Docker compose config；可选 curl 验证 ask 后 Trace 包含 plan/step/observation 摘要。
开始时间：2026-05-31
完成时间：2026-05-31
结果：Agent Runtime 模型、规则模板 Planner、PlanExecutor、AgentService 委托 Runtime、Trace toolName 事件、Runtime/Trace/AgentService 测试和文档同步均已完成；未新增 DDL。
未完成项：未做项目对比报告、前端、RAG、SSE、生产鉴权，均属于阶段 8 明确不做范围。
下一步：阶段 9 `feature/08-tool-runtime` proposal。
```

### 上一阶段记录

```text
阶段：7 - 学习计划与任务持久化
分支：feature/06-learning-plan
目标：根据推荐项目生成 7 天学习任务，持久化学习目标和任务，并支持查询与状态更新。
范围：学习计划领域模型；规则模板生成 + LLM 文案增强 fallback；learning_goal/learning_task Entity/Mapper/Service；/api/agent/ask 追加 learningPlan；/api/learning 查询和状态更新接口；Trace 增强；测试；文档同步。
不做：前端、日历提醒、复杂任务调度、多用户/多租户/登录鉴权、Redis 缓存、RAG、Streaming/SSE、新增数据库迁移框架。
风险：数据库持久化；默认无 MySQL 时 ask 主流程不能退化；LLM 输出不可信；学习任务不得编造项目能力；/api/learning MVP 无鉴权不应公网暴露；状态更新需要枚举校验。
验证命令：Java mvn test；Go go test ./...；Docker compose config；启用 MySQL 后 curl 验证 ask 返回 learningPlan、查询 goal、更新 task status。
开始时间：2026-05-31
完成时间：2026-05-31
结果：学习计划领域模型、规则模板生成器、LLM 可选增强 fallback、learning_goal/learning_task Entity/Mapper/Service、/api/agent/ask learningPlan 响应、/api/learning 查询和状态更新接口、Trace 记录、测试和文档同步均已完成。
未完成项：未做前端、鉴权、多用户隔离、计划版本管理；这些均属于阶段 7 明确不做范围。
下一步：阶段 8 Agent Runtime 内核。
```

### 上一阶段记录

```text
阶段：6 - Spring AI + DeepSeek Agent 编排
分支：feature/05-spring-ai-deepseek-agent
目标：接入 Spring AI ChatClient + DeepSeek V4 Pro，将硬编码模板回答替换为 LLM 自然语言生成，包括目标解释和回答生成。
范围：Spring AI ChatClient 手动配置 Bean（OpenAI 协议 → DeepSeek endpoint）；GoalInterpreter（LLM 提取搜索关键词）；AnswerGenerator（LLM 生成推荐回答）；AgentService 重命名与 LLM 整合；Fallback 策略（无 Key/超时 → 模板）；Trace 增强（LLM 调用记录）；测试；文档同步。
不做：Spring AI Tool Calling（@Tool 注解）、MCP 协议、Streaming/SSE、向量检索/RAG、Redis adapter、前端、生产鉴权。
风险：DeepSeek API 兼容性；ChatClient Bean 条件创建（无 Key 不阻塞启动）；Prompt 长度和注入控制；Token 成本；LLM 输出不可信（评分被改写）；重命名影响面。
验证命令：Java mvn test（LLM 禁用 + 启用）；Go go test ./...；Docker compose config；无 Key curl → 模板回答；有 Key curl → LLM 自然语言回答；超时 → fallback。
开始时间：2026-05-30
完成时间：2026-05-30
    结果：Spring AI ChatClient 配置 Bean 完成（手动创建，无 Key 返回 null）；GoalInterpreter（LLM 提取关键词 + JSON 解析 + fallback）完成；AnswerGenerator（LLM 生成推荐回答 + evidence 截断 + System Prompt 评分保护）完成；AgentService 重命名并整合 LLM（GoalInterpreter + AnswerGenerator 替代模板方法）完成；LLM Trace 记录完成；单元测试（3 个新测试类）完成；文档和 code_copilot 制品已同步。
    未完成项：无；Spring AI Tool Calling、MCP、Streaming/SSE、RAG、前端属于后续阶段。
    下一步：阶段 7 学习计划与任务持久化。
```

### 上一阶段记录

```text
阶段：5 - GitHub 真实 API 集成
分支：feature/04-github-real-api
目标：完善真实 GitHub API 模式，包括 search/profile/readme、限流、错误处理和缓存策略。
范围：Go 结构化错误类型（403/429/404 分离 + Retry-After 解析）；Go 可配置基础设施参数（限流/缓存/并发/超时）；Java CollectorClient 真实 API 方法（search/profile/readme/batchProfile）；Java MockAgentService 真实模式编排（search → enrich → score → respond）；Java 结构化异常层级（RateLimit/CollectorUnavailable/GitHubApi）；文档和 code_copilot 制品同步。
不做：Redis adapter、Spring AI DeepSeek、ETag/304 条件请求（P1 可选项）、learning_goal/learning_task、前端、生产鉴权。
风险：Go 错误响应形状变更可能影响 Java 错误解析；N+1 次 README 获取性能；无 Token 时 GitHub 限流严格（60 req/h）；模式双开关需清晰文档。
验证命令：Go go test ./...；Java mvn test；Docker compose config；Go mock + Java 真实模式 curl /api/agent/ask；Go 不可用 → Java 502；无 Token 真实 API → 合理限流提示。
开始时间：2026-05-30
完成时间：2026-05-30
    结果：Go 结构化错误类型（GitHubError + ErrorResponse）和 403/429/404 分离完成；Go 可配置参数（限流/缓存/并发/超时）完成；Java 4 个新 DTO + CollectorClient 4 个真实 API 方法完成；MockAgentService 真实模式编排（search → readme enrich → score → respond）完成；Java 结构化异常层级（RateLimit/CollectorUnavailable/GitHubApi）和错误解析完成；文档和 code_copilot 制品已同步。阶段 5 知识已沉淀到 knowledge/index.md。
    未完成项：无；Redis adapter、Spring AI DeepSeek、ETag/304、learning_goal/learning_task 属于后续阶段。
    下一步：阶段 6 Spring AI + DeepSeek V4 Pro Agent 编排。
```

## 10. 每阶段执行模板

每个阶段开始时复制以下模板到本文件对应阶段记录中：

```text
阶段：
分支：
目标：
范围：
不做：
风险：
验证命令：
开始时间：
完成时间：
结果：
未完成项：
下一步：
```

## 11. 变更记录

| 日期 | 分支 | 变更 | 结果 |
|---|---|---|---|
| 2026-05-29 | `plan/implementation-progress` | 创建项目实施进度文档 | 已合并到 `main` |
| 2026-05-29 | `main` | 提交 OpenScout MVP 初始骨架 | `a748232` |
| 2026-05-30 | `main` | 安装本地 Go 1.26.3，生成 Go 依赖校验文件，补充工具脚本 | `10221be` |
| 2026-05-30 | `main` | 更新项目实施进度，标记阶段 0-2 当前状态 | 待提交 |
| 2026-05-30 | `feature/02-mock-e2e-demo` | 创建阶段 3 mock 端到端演示 propose | 已完成 |
| 2026-05-30 | `feature/02-mock-e2e-demo` | 完成 Java 调 Go mock 端到端 curl 验证，归档 `openscout-mvp-foundation` | 已完成 |
| 2026-05-30 | `feature/02-mock-e2e-demo` | achieve 沉淀阶段 3 知识，归档 `openscout-mock-e2e-demo` | 已完成 |
| 2026-05-30 | `feature/03-mybatis-persistence` | 创建阶段 4 MyBatis-Plus 持久化 propose | 进行中 |
| 2026-05-30 | `feature/03-mybatis-persistence` | 完成 Task 1-4：Entity/Mapper/Service/持久化写入，Task 5 文档更新 | 进行中 |
| 2026-05-30 | `feature/04-github-real-api` | 创建阶段 5 GitHub 真实 API 集成 propose（spec + tasks） | 进行中 |
| 2026-05-30 | `feature/04-github-real-api` | 完成 Task 1-5：Go 结构化错误、可配置参数、Java DTO/Client/编排/异常层级，Task 6 文档同步 | 进行中 |
| 2026-05-30 | `feature/04-github-real-api` | achieve 沉淀阶段 5 知识，spec 归档为 `done`，知识写入 `code_copilot/knowledge/index.md` | 已完成 |
| 2026-05-30 | `feature/05-spring-ai-deepseek-agent` | 创建阶段 6 Spring AI + DeepSeek Agent 编排 propose（spec + tasks） | 已完成 |
| 2026-05-30 | `feature/05-spring-ai-deepseek-agent` | 完成 Task 1-7：ChatClient 配置、GoalInterpreter、AnswerGenerator、AgentService 重命名与 LLM 整合、Trace 增强、测试、文档同步 | 已完成 |
| 2026-05-30 | `feature/05-spring-ai-deepseek-agent` | review 修复 5 项（代码重复消除、时间度量统一、格式修正、常量命名），achieve 沉淀阶段 6 知识，spec 归档为 `done` | 已完成 |
| 2026-05-31 | `feature/06-learning-plan` | 创建阶段 7 学习计划与任务持久化 propose（spec + tasks + test-spec + log） | 已完成 |
| 2026-05-31 | `feature/06-learning-plan` | 完成 Task 1-6：学习计划生成、持久化、ask 集成、查询/状态 API、测试和文档同步 | 已完成 |
| 2026-05-31 | `feature/06-learning-plan` | review 执行真实环境验证，achieve 沉淀阶段 7 知识，spec 归档为 `done` | 已完成 |
| 2026-05-31 | `main` | 提交并合并阶段 7 `feature/06-learning-plan` | 已合并 |
| 2026-05-31 | `feature/07-agent-runtime` | 将 Agent 推理架构优化方案吸收到项目实施进度，统一阶段 8-15 路线 | 已完成 |
| 2026-05-31 | `feature/07-agent-runtime` | 创建阶段 8 Agent Runtime 内核 proposal（spec + tasks + test-spec + log） | 已完成 |
| 2026-05-31 | `feature/07-agent-runtime` | 完成 Task 1-6：Agent Runtime、PlanExecutor、Trace toolName 事件、测试和文档同步 | 已完成 |
| 2026-05-31 | `feature/07-agent-runtime` | achieve 沉淀阶段 8 知识，spec 归档为 `done` | 已完成 |
| 2026-05-31 | `main` | 提交并合并阶段 8 `feature/07-agent-runtime` | 已合并 |
| 2026-05-31 | `feature/08-tool-runtime` | 创建阶段 9 Tool Runtime proposal（spec + tasks + test-spec + log） | 已完成 |
| 2026-05-31 | `feature/08-tool-runtime` | 完成 Task 1-6：Tool Runtime、6 个固定 Tool、Trace Tool 事件、PlanExecutor 委托、测试和文档同步 | 已完成 |
| 2026-05-31 | `feature/08-tool-runtime` | review 并 achieve 沉淀阶段 9 知识，spec 归档为 `done` | 已完成 |
| 2026-05-31 | `main` | 提交并合并阶段 9 `feature/08-tool-runtime` | 已合并 |
| 2026-05-31 | `feature/09-project-memory-rag` | 创建阶段 10 Project Memory / RAG proposal（spec + tasks + test-spec + log） | 已完成 |
| 2026-05-31 | `feature/09-project-memory-rag` | 完成 Task 1-9：ProjectMemoryService、CheckMemoryTool、Planner/Search/Fetch/Score 修改、50 tests 和文档同步 | 已完成 |
| 2026-05-31 | `feature/09-project-memory-rag` | review 修复 6 项（hasExamples 正则、readmeLength 推断、死参数、未用导入、reduce→joining、hasDocker DRY） | 已完成 |
| 2026-05-31 | `main` | 提交并合并阶段 10 `feature/09-project-memory-rag`，归档为 `done` | 已合并 |
| 2026-06-01 | `feature/10-evidence-react` | 完成阶段 11 Evidence ReAct apply/review/fix/achieve，Java 70 tests、Go tests、Docker compose config 通过 | 已完成 |
| 2026-06-01 | `main` | 提交并合并阶段 11 `feature/10-evidence-react`，归档为 `done` | 已合并 |
| 2026-06-01 | `feature/11-reflection-verifier` | 完成阶段 12 Reflection Verifier apply（VerifyAnswerTool + 三项 checker + 28 tests），Java 98 tests 通过 | 已完成 |
| 2026-06-01 | `feature/11-reflection-verifier` | 完成阶段 12 7 轮 review（共 28 项修复），achieve 沉淀知识，待合并 | 已完成 |

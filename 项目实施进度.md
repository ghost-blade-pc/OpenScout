# OpenScout 项目实施进度

> 本文档用于跟踪 OpenScout 从 0 到 1 的实施方案、阶段进度和分支规划。  
> 后续每完成一个阶段，都需要同步更新本文档，并在进入下一阶段前确认范围、验收标准和风险。

## 1. 使用原则

- 每一个实施阶段对应一个独立 Git 分支。
- 每个阶段先明确目标、范围、验收标准，再进入编码或文档修改。
- 阶段完成后需要记录验证结果、未完成项、后续调整建议。
- `main` 只作为稳定基线分支；功能开发、验证、修复和方案调整都在分支中完成。
- `code_copilot/changes/` 记录具体 change 的 spec、tasks、test-spec、log；本文档记录项目整体路线和跨阶段进度。
- `OpenScout Agent 项目方案.md` 是原始方案输入文件，不加入 Git。

## 2. 当前 Git 状态

- 当前分支：`plan/implementation-progress`
- 当前用途：创建项目实施进度总文档，建立后续阶段分支规划。
- 当前仓库状态：尚未创建首个 commit，已有 MVP 骨架文件处于未跟踪状态。
- 当前 Git 忽略规则：`OpenScout Agent 项目方案.md` 已通过 `.gitignore` 排除。

## 3. 阶段状态说明

| 状态 | 含义 |
|---|---|
| 待确认 | 阶段目标和验收标准还需要确认 |
| 待开始 | 阶段已确认，但尚未创建或切换到对应分支实施 |
| 进行中 | 已在对应分支实施 |
| 待验证 | 代码或文档已完成，但验证命令未全部通过 |
| 已完成 | 阶段目标完成，验证结果已记录 |
| 已合并 | 阶段分支已合并回 `main` |
| 延后 | 暂不进入当前从 0 到 1 主线 |

## 4. 从 0 到 1 实施路线

| 步骤 | 分支 | 阶段目标 | 当前状态 | 验收标准 |
|---|---|---|---|---|
| 0 | `plan/implementation-progress` | 建立总进度文档和分支规划 | 进行中 | 根目录存在本文档，后续阶段有清晰分支和验收口径 |
| 1 | `feature/00-mvp-foundation` | 创建 Java Agent Server、Go Repo Collector、部署脚本、README 和 Demo 文档 | 待验证 | Java `mvn test` 通过；Docker Compose config 通过；Go 工具链验证补齐 |
| 2 | `feature/01-go-collector-verification` | 安装/确认 Go 工具链后，格式化并验证 Go Collector | 待开始 | `gofmt -w cmd internal`、`go test ./...`、`go run ./cmd/server` 通过 |
| 3 | `feature/02-mock-e2e-demo` | 跑通 Java 调 Go 的 mock 端到端演示 | 待开始 | Go `/api/repos/mock` 和 Java `/api/agent/ask` curl 验证通过，Trace 可查询 |
| 4 | `feature/03-mybatis-persistence` | 将项目画像、分析结果和 Trace 从内存实现推进到 MyBatis-Plus 持久化 | 待确认 | 表结构、Mapper、Service、单测或集成验证完成 |
| 5 | `feature/04-github-real-api` | 完善真实 GitHub API 模式，包括 search/profile/readme、限流、错误处理和缓存策略 | 待确认 | 无 Token mock 可用；有 Token 真实 API 可用；403/429 处理清晰 |
| 6 | `feature/05-spring-ai-deepseek-agent` | 接入 Spring AI + DeepSeek V4 Pro，将 mock Agent 替换为真实 Agent 编排 | 待确认 | 未配置 Key 时可 fallback；配置 Key 后能完成工具调用或等价编排 |
| 7 | `feature/06-learning-plan` | 生成并持久化学习路径与任务 | 待确认 | 根据推荐项目生成 7 天任务，支持查询和状态更新 |
| 8 | `feature/07-project-compare-report` | 增强多项目对比、报告输出和简历/面试表达 | 延后 | 输出可解释对比报告，评分 evidence 不被模型改写 |

## 5. 当前已完成事实

以下事实来自当前工作区和 `code_copilot/changes/openscout-mvp-foundation`：

- 已创建 `code_copilot/` 工作区、根级 `AGENTS.md`、`CLAUDE.md`。
- 已创建 `.gitignore`，排除 `/OpenScout Agent 项目方案.md`。
- 已创建 Java 服务 `openscout-agent-server/`。
- 已创建 Go 服务 `openscout-repo-collector/`，Go module 为 `github.com/LiPeicheng/openscout-repo-collector`。
- 已创建 `deploy/docker-compose.yml` 和 `deploy/init.sql`。
- 已创建根级 `README.md` 和 `docs/` 演示文档。
- Java 验证 `cd openscout-agent-server && mvn test` 已通过。
- Docker Compose 配置验证 `docker compose -f deploy/docker-compose.yml config` 已通过。
- Go 验证当前受阻：本机缺少 `go` 和 `gofmt`。

## 6. 下一步确认点

进入下一阶段前，需要确认：

- 是否先将当前未跟踪文件整理为初始 commit。
- `feature/00-mvp-foundation` 是否作为 MVP 骨架正式分支，还是将当前工作区先提交到 `plan/implementation-progress` 后再拆分。
- 本机是否安装 Go，或者是否允许通过 Docker/CI 验证 Go Collector。
- 是否优先补 Go 验证，还是先做 Java + Go mock 端到端演示。

## 7. 每阶段执行模板

每个阶段开始时复制以下模板到本文件对应阶段记录中：

```text
阶段：
分支：
目标：
范围：
不做：
风险：
验证命令：
开始时间：
完成时间：
结果：
未完成项：
下一步：
```

## 8. 变更记录

| 日期 | 分支 | 变更 | 结果 |
|---|---|---|---|
| 2026-05-29 | `plan/implementation-progress` | 创建项目实施进度文档 | 进行中 |
